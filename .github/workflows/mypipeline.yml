name: My First Pipeline

on:
  push:
    branches: [ master ]

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "My pipeline is working!"

      - name: Show date
        run: date

      - name: Show files
        run: ls -la
